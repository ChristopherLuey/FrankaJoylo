name: franka_joylo
channels:
  - defaults
dependencies:
  - python=3.10
  - numpy
  - pip
  - pip:
      - dynamixel-sdk
      - mujoco>=3.0
      - pyyaml
      - easydict
      - termcolor
      - pyzmq
      - protobuf<4
      - pybullet
      - -e .
